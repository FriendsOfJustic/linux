spring:
  application:
    name: geohazard-chain
  datasource:
    mysql:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://geohazard-mysql:3306/geohazard?useSSL=false&useUnicode=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8
      username: root
      password: Mining2021@zhq
    pgsql:
      #      注意：这里 currentSchema 必须设置为 public ，不然没有办法使用空间函数 "ST_*"
      url: jdbc:postgresql://geohazard-postgis:5432/geohazard?currentSchema=public
      username: postgres
      password: geohazard
      driver-class-name: org.postgresql.Driver
    hikari:
      max-lifetime: 6000000 # 6000 s 100 minute

server:
  port: 20231
  servlet:
    context-path: /geohazard-chain

mybatis:
  mapper-locations: classpath:mysqlMapper/*Mapper.xml,classpath:pgsqlMapper/*Mapper.xml,
  configuration:
    map-underscore-to-camel-case: true
eureka:
  client:
    serviceURL:
      defaultZone: http://geohazard-eureka:20222/eureka/
  instance:
    hostname: geohazard-chain




geoserver:
  username: admin
  password: geoserver
  hostname: geohazard-geoserver
  port: 8080


geoserverURL:
  IP: www.rsgisai.com
  PORT: 8080



Module4:
  PATH: /home/code_space/py_test_demo/DEM51.tif

logging:
  level:
    com:
      rsgisai:
        geohazard:
          level: DEBUG
