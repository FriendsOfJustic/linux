eureka:
  client:
    serviceURL:
      defaultZone: http://localhost:20222/eureka/
  instance:
    hostname: geohazardMain



spring:
  application:
    name: geohazardMain
  datasource:
    driver-class-name: org.postgresql.Driver
    username: geohazard
    password: cumtb_2023_rerssil
    url:  jdbc:postgresql://122.112.250.145:15432/geohazard?currentSchema=public



server:
  port: 20888
  servlet:
    context-path: /geohazardmain


# 配置 feign 默认请求时间仅几秒钟，配置请求时间长一些(毫秒)
# 一定要设置这个时间，否则几秒内连接就会断开
feign:
  client:
    config:
      default:
        connectTimeout: 600000
        readTimeout: 600000     #等待时间设置为10min 后面还可以延长 - TODO
  hystrix:
    enabled: false    # 不要开启hystrix，会导致超时配置不生效